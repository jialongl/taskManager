enum commandMethod {
	ADD,
	EDIT,
	RM,
	LS,
	TASK,
	PRI,
	CRON,
	FINISH,
	EXPORT,
	IMPORT
};

enum sortKeyword{
	DEADLINE,
	PRIORITY,
	SERIAL_NUMBER
};

enum filterType{
	K_FILTER,
	S_FILTER,
	F_FILTER,
	G_FILTER,
	OVERDUE_FILTER
};
